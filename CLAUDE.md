# Repository instructions

## Git workflow

Merge changes directly to `main` rather than opening a pull request, unless
the user explicitly asks for a PR (e.g. for review, or because CI/checks
should gate the change).
